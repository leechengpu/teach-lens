# macOS Apple Silicon 本機加速依賴
# 雲端部署不需要安裝（Streamlit Cloud 為 Linux 環境）
#
# 安裝方式：
#   source .venv/bin/activate
#   pip install -r requirements-mac.txt
#
# 安裝後系統會自動偵測並啟用 MLX Whisper 引擎，
# 1 分鐘音檔轉錄 < 5 秒（M2/M3 實測）。
mlx-whisper>=0.4.0
